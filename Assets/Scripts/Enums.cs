public enum CameraView
{
    Side = 0,
    Rear,
}

public enum PlayEventType
{
    EnemyRaid = 0,
}

public enum UnitState
{
    Continue = 0,
    Idle,
    Run,
    Attack_1,
    Attack_2,

    Strike,
    Jump,
    Die,
}